import 'package:flutter/material.dart';

class CustomerDriverHeaderView extends StatelessWidget {
  const CustomerDriverHeaderView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [

      ],
    );
  }
}
